{
  "name": "ccc-admin-server",
  "version": "1.0.0",
  "description": "API and database for CCC Admin",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "prisma migrate dev --name init",
    "seed": "node prisma/seed.js",
    "studio": "prisma studio"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-session": "^1.18.1"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  }
}
